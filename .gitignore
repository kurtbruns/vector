.DS_Store
.vscode
node_modules
builds
cache
types
docs
dist
lib

# TODO: remove temporary ignore
examples.ignore
website

src/**/*.js
src/**/*.map.js

# This project is npm-only; package-lock.json is the single lockfile.
# A stray yarn.lock resolves a divergent tree that `npm audit` never sees,
# and yarn 1's audit endpoint was retired by the registry in July 2026.
yarn.lock
.yarn/
.pnp.*
